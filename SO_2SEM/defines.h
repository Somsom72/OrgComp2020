/**
 * \file defines.h
 * \brief Guarda todos defines do projeto.
 */

#ifndef DEFINES_H
#define DEFINES_H

    #define FATOR_INERCIA 3
    #define MAX_CARACTERES 15
    #define DURACAO_DO_JOGO 20
    #define TAMANHO_DA_CORDA 15
    #define TRUE 1
    #define FALSE 0
    #define MAX_ITENS 2 * DURACAO_DO_JOGO //Número máximo de itens que podem ser consumidos ou produzidos
    #define max(a,b) ((a) > (b) ? (a) : (b))
    #define min(a,b) (((a)<(b))?(a):(b))
    #define boolean unsigned char

#endif